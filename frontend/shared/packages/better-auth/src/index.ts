// Compatibility wrapper for @dotmac/better-auth when resolved via shared/packages.
// Re-export the shared lib implementation so Jest and tooling can resolve correctly.
export * from "../../../lib/better-auth";

