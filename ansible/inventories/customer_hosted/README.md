# Customer-Hosted Inventory

When a tenant elects to run the DotMac stack on their own infrastructure the
provisioning pipeline builds an inventory that targets the provided host(s).
This placeholder illustrates the structure expected by the Ansible playbooks
and AWX job template.
